[project]
name = "face-recognition"
version = "2.0.0"
description = "全栈人脸识别打卡系统 - Full-stack Face Recognition Attendance System"
requires-python = ">=3.9"

[project.scripts]
face-recognition = "src.main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
